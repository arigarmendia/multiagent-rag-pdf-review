[project]
name = "multiagent-rag-pdf-review"
version = "0.1.0"
description = "Multiagent pipeline with RAG for error detection in academic PDF documents"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "streamlit",
    "pymupdf",
    "pdfplumber",
    "transformers",
    "torch",
    "chromadb",
    "sentence-transformers",
    "ollama",
    "language-tool-python",
    "uvicorn",
    "python-dotenv",
    "python-multipart",
    "pydantic",
    "mlflow>=2.13",
    "pytest",
    "pytest-asyncio",
    "torchvision>=0.27.0",
    "timm>=1.0.27",
    "groq>=1.4.0",
    "openai>=2.41.0",
    "guardrails-ai>=0.10.2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]
